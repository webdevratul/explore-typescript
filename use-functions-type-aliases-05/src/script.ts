// let myFunc: Function;

// myFunc = () => {};

// wrong karon amra atar type ta function bole diyechi
// myFunc = 2;

/* ==================================================== */

// const myFunc2 = (a: string, b: string, c?: string /*= "true"*/) => {
//   console.log(`Hello ${a} ${b}`);
// };

// jehetu parametr a amra bole diyechi string type tai argument hisebe amader sob somoy string e pathate hobe. c hocche optional parameter.chaile amra akta default value set kore dite pari taile er optional bolar pryojon nai

// myFunc2("this is", "Mahibul Islam");

/* ==================================================== */

// Type Aliases. mani amra nijeder moto kore custom type define korte pari
// type stringOrNum = string | number;
// type userType = { name: string; age: number };

// const userDetails = (id: stringOrNum, user: userType) => {
//   console.log(id);
//   console.log(user);
// };

// const myObj = {
//   name: "Ratul",
//   age: 25,
// };

// userDetails(10, myObj);

/* ==================================================== */
